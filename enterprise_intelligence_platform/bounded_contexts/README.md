# bounded_contexts

This package is the entry point for bounded-context implementation inside the existing Frappe app.

## Sprint 0 rule
- Keep this package minimal.
- Add concrete bounded-context subpackages only when their Sprint work starts.
- Do not add business logic at this stage.

## Architectural alignment
Context ownership and boundaries are defined in approved architecture documents (especially `docs/MASTER_ARCHITECTURE.md` and Phase 5 artifacts).
